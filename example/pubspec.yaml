name: example
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.1.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  async_event_loader:
    path: ../../async_event_loader

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
